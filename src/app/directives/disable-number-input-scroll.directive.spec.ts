import { DisableNumberInputScrollDirective } from './disable-number-input-scroll.directive';

describe('DisableNumberInputScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableNumberInputScrollDirective();
    expect(directive).toBeTruthy();
  });
});
